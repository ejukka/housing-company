language: node_js
node_js: '12'
cache: yarn

before_install:
  - yarn install
  - yarn run create

jobs:
  include:
    - stage: linting
      script: yarn prettier
    - stage: unit testing
      script: yarn test -u
